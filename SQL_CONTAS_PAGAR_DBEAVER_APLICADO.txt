SQL DE CONTAS A PAGAR APLICADO

Foi utilizado exatamente o script enviado e validado no DBeaver.

Únicas alterações:
- DATE '2026-07-01' -> CAST(:data_inicio AS date)
- DATE '2026-07-10' -> CAST(:data_fim AS date)

Todo o restante do SQL foi preservado, incluindo CTEs, joins, aliases,
formatação e filtro pela data de vencimento.

A execução continua usando o cursor nativo da mesma conexão PostgreSQL
das demais fontes.

O cache anterior de Contas a Pagar foi removido para uma nova carga.
