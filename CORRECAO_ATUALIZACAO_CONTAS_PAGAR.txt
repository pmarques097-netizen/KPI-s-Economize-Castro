CORREÇÃO APLICADA

O teste da consulta funcionava, mas a atualização falhava porque a tabela
analise_posicao_resumo não existia no SQLite.

Agora:
- todas as tabelas analíticas são criadas automaticamente;
- a estrutura é validada antes de salvar qualquer snapshot;
- a estrutura é validada antes de carregar a Análise Comercial;
- o SQL e a conexão de Contas a Pagar permanecem inalterados;
- a atualização deve concluir e salvar os registros no cache.
